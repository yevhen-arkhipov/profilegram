import { View, Image } from 'react-native';

const GalleryBtn = () => {
  return (
    <View>
      <Image source={require('../assets/icon/gridIcon.png')} />
    </View>
  );
};

export default GalleryBtn;

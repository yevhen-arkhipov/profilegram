import { View, Image } from 'react-native';

const ProfileBtn = () => {
  return (
    <View>
      <Image source={require('../assets/icon/profileIcon.png')} />
    </View>
  );
};

export default ProfileBtn;

import { View, Image } from 'react-native';

const CreateGrayBtn = () => {
  return (
    <View>
      <Image source={require('../assets/icon/createIcon.png')} />
    </View>
  );
};

export default CreateGrayBtn;
